{
  "description": "RAG Web Browser - Web scraping tool for RAG pipelines.",
  "b": "query",
  "properties": [
    {
      "title": "Query",
      "name": "query",
      "type": "array",
      "editor": "requestList",
      "description": "Search keywords or URL",
      "default": [
        { "url": "https://example.com" }
      ],
      "required": true
    }
  ]
}
