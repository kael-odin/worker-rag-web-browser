{
  "description": "RAG Web Browser - A powerful web scraping tool that extracts content from websites and converts it to structured formats like Markdown. It supports multiple scraping tools, configurable timeouts, and various output formats for RAG (Retrieval-Augmented Generation) applications.",
  "b": "query",
  "properties": [
    {
      "title": "Query URL",
      "name": "query",
      "type": "array",
      "editor": "requestList",
      "description": "The search query or URL to scrape. Enter one URL per line.",
      "default": [
        { "url": "https://example.com" }
      ],
      "required": true
    },
    {
      "title": "Max Results",
      "name": "maxResults",
      "type": "integer",
      "editor": "number",
      "description": "Maximum number of results to return from the search.",
      "default": 3
    },
    {
      "title": "Output Format",
      "name": "outputFormat",
      "type": "string",
      "editor": "select",
      "description": "The format of the output content.",
      "default": "markdown",
      "allowedValues": [
        { "value": "markdown", "label": "Markdown" },
        { "value": "text", "label": "Plain Text" },
        { "value": "html", "label": "HTML" }
      ]
    },
    {
      "title": "Scraping Tool",
      "name": "scrapingTool",
      "type": "string",
      "editor": "select",
      "description": "The tool to use for scraping web content.",
      "default": "raw-http",
      "allowedValues": [
        { "value": "raw-http", "label": "Raw HTTP (Fast)" },
        { "value": "browser-playwright", "label": "Browser (Playwright)" }
      ]
    },
    {
      "title": "Request Timeout (seconds)",
      "name": "requestTimeoutSecs",
      "type": "integer",
      "editor": "number",
      "description": "Maximum time in seconds to wait for a request to complete.",
      "default": 40
    },
    {
      "title": "Remove Cookie Warnings",
      "name": "removeCookieWarnings",
      "type": "boolean",
      "editor": "switch",
      "description": "Automatically remove cookie warning banners from pages.",
      "default": true
    },
    {
      "title": "Debug Mode",
      "name": "debugMode",
      "type": "boolean",
      "editor": "switch",
      "description": "Enable debug logging for troubleshooting.",
      "default": false
    }
  ]
}
